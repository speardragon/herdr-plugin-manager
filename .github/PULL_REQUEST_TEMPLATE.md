## What this changes

<!-- The behaviour before and after, in a sentence or two. -->

## Why

<!-- The problem it solves. Link the issue if there is one: Fixes #123 -->

## How you verified it

<!--
What you actually ran, and what you saw. A trace from a real reproduction is
worth more than a description of one.
-->

- [ ] `bash tests/run.sh` passes
- [ ] Tried it in a real popup (`herdr plugin action invoke ray.plugin-manager.open`)

<!-- If this changes update checks or install behaviour, say which ref kinds you
covered: default branch, named branch, lightweight tag, annotated tag, exact-sha
pin, locally linked. They fail differently. -->

## Environment

- herdr:
- plugin-manager:
- OS / bash:
